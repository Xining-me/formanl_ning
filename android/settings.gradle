pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url "https://storage.googleapis.com/flutter_infra_release/maven" }
    }
    plugins {
        id("dev.flutter.flutter-plugin-loader") version "1.0.0"
    }
}

rootProject.name = "campustweet"
include ":app"

